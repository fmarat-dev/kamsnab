// Сидинг статичных инфостраниц (delivery/payment/service/repair) в коллекцию
// pages. Идемпотентно — пропускает страницы, у которых slug уже существует.
//
// ВАЖНО: этот файл раньше также содержал сидинг категорий/товаров/settings
// демо-данными с раннего этапа проекта (до полного переноса каталога с
// oxlift.ru через schema/oxlift/scrape.mjs). Эта часть была неидемпотентна
// по отношению к реальному каталогу (проверяла свои же slug'ы вроде
// "electric"/"diesel"/"stackers", а не реальную таксономию) и при повторном
// запуске создавала дублирующиеся категории/товары поверх настоящего
// каталога. Убрана намеренно — не восстанавливать без переработки.
//
// Запуск: pnpm --filter @kamsnab/cms content:seed
import { createDirectus, rest, staticToken, createItem, readItems } from "@directus/sdk";

const url = process.env.PUBLIC_URL ?? "http://localhost:8055";
const email = process.env.ADMIN_EMAIL;
const password = process.env.ADMIN_PASSWORD;

const client = createDirectus(url).with(rest());

async function login() {
  const res = await fetch(`${url}/auth/login`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ email, password })
  });
  const { data } = await res.json();
  return data.access_token;
}

async function main() {
  const token = await login();
  const authed = client.with(staticToken(token));

  const existingPages = await authed.request(readItems("pages", { limit: -1 }));
  const pageDefs = [
    {
      slug: "delivery",
      title: "Доставка",
      content:
        "<h2>Доставим технику с ближайшего к вам склада в течение 7 дней</h2>" +
        "<p>Собственная логистическая служба оптимизирует расходы и сроки доставки спецтехники за счёт:</p>" +
        "<ul><li>Профессионального подбора транспорта</li><li>Многолетнего опыта логистов</li>" +
        "<li>Собственного автопарка</li><li>Автоматизированного трекинга</li></ul>" +
        "<p>Оптимизированные маршруты, экономия до 50% на логистике, отправки по всей России ежедневно.</p>"
    },
    {
      slug: "payment",
      title: "Оплата",
      content:
        "<h2>Оплата техники</h2>" +
        "<p>Компания является аккредитованным поставщиком большинства лизинговых компаний России. " +
        "Покупка техники в лизинг доступна для юридических лиц (ИП и ООО) и некоммерческих организаций " +
        "с положительной кредитной историей.</p>" +
        "<ul><li>Приобретение техники в лизинг через официальных партнёров</li>" +
        "<li>Оплата банковским переводом для юридических лиц или НКО</li>" +
        "<li>Оплата наличными или картой в офисе или на складе компании</li></ul>" +
        "<h3>Как проходит лизинговая сделка</h3>" +
        "<ol><li>Выявляем потребность и делаем ценовое предложение</li>" +
        "<li>Передаём заявку в лизинговые компании для нескольких графиков платежей</li>" +
        "<li>После оформления сделки и оплаты отправляем технику на предпродажную подготовку</li>" +
        "<li>Отгружаем выбранным способом — доставка или самовывоз</li></ol>"
    },
    {
      slug: "service",
      title: "Сервис и гарантия",
      content:
        "<h2>Гарантия 12 месяцев или 2000 моточасов</h2>" +
        "<p>На всю технику действует гарантия 12 месяцев или 2000 моточасов — распространяется только на технику, " +
        "продаваемую официальным дистрибьютором или дилером.</p>" +
        "<ul>" +
        "<li><strong>Обращение в сервисный центр</strong> — бесплатное гарантийное обслуживание или ремонт техники</li>" +
        "<li><strong>Выезд сервисной бригады</strong> — плановое ТО или ремонт на месте в течение 5 дней</li>" +
        "<li><strong>Самостоятельное обслуживание</strong> — без потери гарантии, с консультацией сервисных инженеров</li>" +
        "</ul>"
    },
    {
      slug: "repair",
      title: "Ремонт и обслуживание техники",
      content:
        "<h2>Профессиональный ремонт складской и подъёмной техники</h2>" +
        "<p>Выполняем плановое техническое обслуживание и срочный ремонт вилочных погрузчиков, штабелёров, " +
        "ричтраков, тележек и другого складского оборудования — с использованием оригинальных запчастей и " +
        "диагностического оборудования.</p>" +
        "<h3>Ремонт погрузчиков</h3>" +
        "<ul>" +
        "<li>Дизельные погрузчики — двигатель, трансмиссия, гидравлика, система охлаждения</li>" +
        "<li>Электропогрузчики — тяговые двигатели, контроллеры, аккумуляторные батареи</li>" +
        "<li>Газобаллонные погрузчики</li>" +
        "</ul>" +
        "<h3>Штабелёры и ричтраки</h3>" +
        "<p>Подъёмные механизмы, гидроцилиндры мачты, системы управления, ходовая часть и тормозная система.</p>" +
        "<h3>Ручные гидравлические тележки</h3>" +
        "<p>Ремонт насоса, замена уплотнений, обслуживание колёс и подшипников.</p>" +
        "<h3>Клининговое оборудование</h3>" +
        "<p>Восстановление поломоечных машин, систем подачи воды и моющего средства, фильтрации.</p>" +
        "<h3>Почему ремонт стоит доверить именно КАМСНАБ?</h3>" +
        "<ul>" +
        "<li><strong>Оригинальные запчасти</strong> — используем только рекомендованные комплектующие со своего склада.</li>" +
        "<li><strong>Диагностика с гарантией</strong> — находим коренную причину поломки, а не просто её симптомы. Даём гарантию на выполненные работы.</li>" +
        "<li><strong>Прозрачность</strong> — перед началом работ предоставляем точную смету и согласовываем её с вами.</li>" +
        "<li><strong>Скорость</strong> — благодаря наличию большинства запчастей на складе и опыту инженеров ремонт выполняется в сжатые сроки.</li>" +
        "</ul>"
    }
  ];

  for (const def of pageDefs) {
    const found = existingPages.find((p) => p.slug === def.slug);
    if (found) continue;
    await authed.request(createItem("pages", def));
    console.log(`+ page "${def.title}"`);
  }

  console.log("Готово.");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
